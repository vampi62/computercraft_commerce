function affichage(numero_box)
    if pagebox[numero_box] == 0 then
        modele_menu(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 11 then
        modele_inscription_mdp(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 12 then
        modele_inscription_confirme_mdp(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 20 then
        modele_login(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
    if pagebox[numero_box] == 10 then
        modele_inscription_pseudo(monitor_box[numero_box],numero_box)
    end
end